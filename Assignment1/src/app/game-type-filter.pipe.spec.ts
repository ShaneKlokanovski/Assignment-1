import { GameTypeFilterPipe } from './game-type-filter.pipe';

describe('GameTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GameTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
