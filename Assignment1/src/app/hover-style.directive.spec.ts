import { HoverStyleDirective } from './hover-style.directive';

describe('HoverStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverStyleDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
